package cs5004.animator.controller;

/**
 * This interface mediates between the IAnimatorModel and the IAnimatorView interfaces.
 * It coordinates between the view of the animation and the animator's data.
 * Manages user input and data from the model.
 */
public interface IAnimatorController {

  void go();

}
